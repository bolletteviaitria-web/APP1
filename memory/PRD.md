# TerracitoAppartments — Product Requirements

## Original Problem Statement
Sito web completo per gestione di multiple case vacanza in Italia. Posizionamento: case **ben curate, pulite e accoglienti** — comfort autentico, non lusso ostentato. Multilingua IT/EN, prenotazione diretta con calcolo prezzi, integrazione Stripe, contatto WhatsApp, dashboard admin completa, sincronizzazione iCal con Airbnb/Booking. Si parte da 5 case estendibili tramite admin.

## Stack
- **Frontend**: React + Tailwind v3 + shadcn/ui, react-i18next (IT/EN), date-fns
- **Backend**: FastAPI + MongoDB (Motor async) + APScheduler (background jobs)
- **Integrazioni**: Stripe (payments), Emergent Object Storage (immagini proprietà + documenti ID), iCalendar parsing/export
- **Brand**: TerracitoAppartments (rebranded da VacayStay 2026-04-30)

## Design — Light Mediterranean Luxury
- Background `#F6F4F0` cream / Foreground `#1E232B` charcoal
- Primary `#D26A45` terracotta (HSL: 21 65% 52%)
- Tipografia: Cormorant Garamond (display) + Manrope (body)
- Asymmetric hero, flat cards, micro-animations
- Tutti i token CSS in `/app/frontend/src/index.css`; design system in `/app/design_guidelines.md`

## Implemented Features

### Pubblico
- HomePage con hero, 4 proprietà in evidenza, feature section
- PropertiesPage con filtri (città, ospiti, prezzo)
- PropertyDetailPage con galleria, amenity, calendario disponibilità, recensioni, calcolo prezzo dinamico
- BookingPage con form ospite, scelta full/deposit, redirect Stripe
- BookingSuccessPage con polling pagamento + upload documenti d'identità
- ContactPage con form + WhatsApp CTA
- LegalPages (Privacy, Termini)
- Auth (Login/Register) con JWT, banner demo admin

### Admin Dashboard (`/admin`)
- **Stats**: proprietà totali, prenotazioni totali, in attesa, ricavi mensili, prossimi check-in/out
- **Properties CRUD completo**: PropertyFormDialog con sezioni Basic / Translations IT-EN / Location / Capacity / Pricing / Photos (upload + URL paste + reorder + cover label) / Amenities (multi-toggle) / Seasons / Extras
- **Bookings**: tabella con conferma/cancella/completa + dialog "Documenti Ospite"
- **Contacts**: lista messaggi
- **Sync iCal**: Add feed (Airbnb/Booking/Vrbo/Other), tabella stati, run/delete manuale, copy export URL per piattaforma

### Backend APIs
- Auth (`/api/auth/*`)
- Properties CRUD (`/api/properties/*`) + upload immagine (`/api/admin/properties/upload-image`) + serving pubblico (`/api/property-images/{id}`)
- Bookings (`/api/bookings/*`) + calcolo prezzi (`/api/properties/calculate-price`) + disponibilità (`/api/properties/{id}/availability`)
- Stripe (`/api/payments/*`, webhook)
- Reviews (`/api/reviews/*`)
- iCal sync (`/api/ical-sync*`) + export pubblico (`/api/ical-export/{property_id}.ics`)
- Documenti ospite (`/api/bookings/{id}/documents`) + admin (`/api/admin/documents/{id}/{download|delete}`)
- Contact (`/api/contact`) + Admin dashboard (`/api/admin/dashboard`)

### Background Jobs
- APScheduler AsyncIO: `sync_all_feeds_job` ogni 30 min — pulla tutti i feed iCal e popola la collection `ical_events` (usata in availability)

## Data Models (MongoDB)
- `users`, `properties`, `bookings`, `payment_transactions`, `reviews`
- `ical_syncs` (config), `ical_events` (cache eventi parsati)
- `booking_documents` (con `is_deleted` soft-delete)
- `property_images` (con `is_deleted` soft-delete)
- `contacts`

## Changelog
- **2026-05-02** — Iteration 20: **3 feature richieste dall'utente**. (a) **Nuova route `/prenota/:slug`** con prefill via query params (`?checkin=YYYY-MM-DD&checkout=YYYY-MM-DD&guests=N`): nuovo componente `BookingShortcut.js` che risolve la property via slug, chiama `/api/properties/{id}/calculate-price`, e naviga su `/booking` passando lo stato normale — zero duplicazione della BookingPage. Aggiunto al prompt AI l'istruzione di mandare questo link quando il guest dice "procediamo". Verificato: su `"Ok procediamo"` l'AI emette correttamente `/prenota/villa-smeraldo?checkin=2026-05-20&checkout=2026-05-23&guests=4`. In `ChatWidget.js` nuovo helper `renderMessageContent` che linkifica `/prenota/...` e URL assoluti nei messaggi AI. (b) **Date selezionate verde scuro** nel Calendar di PropertyDetailPage: override di `day_selected` / `day_range_start` / `day_range_end` / `day_range_middle` con `hsl(145 55% 28%)` (scuro per gli estremi) e `hsl(145 45% 85%)` (chiaro per il middle). (c) **Download documenti chat con filename pretty**: `GET /api/admin/chat/documents/{session}/{doc}/download` ora restituisce Content-Disposition `"attachment; filename=\"{property_slug}_{dates|upload_day}_{original_name}.{ext}\""` (es. `villa-smeraldo_2026-05-03_test.pdf`). (d) **Colonna "Doc" in tab Lead Chat admin**: per ogni lead con `has_documents=true` mostra count cliccabile che apre un Dialog con lista documenti, ricerca (nome/tipo/data) e bottone Scarica (parse Content-Disposition lato client per ottenere il nome pretty). Screenshot di verifica acquisito: date 20 e 23 (estremi) in verde scuro, 21-22 in verde chiaro, disponibili in verde-molto-chiaro.

- **2026-05-02** — Iteration 19: **Code review fix #2 (mirato)**. (a) Creato `/app/frontend/src/lib/logger.js` con `log`/`warn`/`error` — le prime due sono no-op in produzione per evitare information disclosure e piccolo overhead; `error` resta attivo per Sentry/DevTools. (b) Sostituite tutte le 15 occorrenze di `console.warn(...)` in `ChatWidget.js`, `AuthContext.js`, `BookingPage.js` con `warn(...)` dal logger (import automatico aggiunto in cima). (c) Risolto unico ternario annidato **vero** in `AdminDashboard.js:623` (badge Attivo/Inattivo) con IIFE + early-return. Gli altri "ternari" segnalati dal review non sono annidati (sono ternari semplici dentro JSX testuali — falso positivo). (d) **Verificato** che l'anti-pattern `is "value"` NON esiste in codice: tutti gli `is`/`is not` sono `is None`/`is not None` (pattern corretto Python, falso positivo del review). Altri punti del review (hook deps, localStorage → cookies, split componenti, refactor complessità Python) erano già ricorrenti dall'iter.12 e restano registrati in "Pending / Future" — richiedono iterazioni dedicate con testing agent, non appaiono qui come quick fix.

- **2026-05-02** — Iteration 18: **Allegati documenti nella chat**. L'ospite può caricare un documento (JPG/PNG/WEBP/HEIC/PDF, max 10 MB) direttamente dal widget chat senza dover completare prima una prenotazione. Nuovo endpoint pubblico `POST /api/chat/upload-document` (multipart: session_id, file, optional property_id) — autenticazione tramite session_id invece di JWT per consentire upload pre-prenotazione. File salvati su Emergent Object Storage sotto `chat-documents/{session_id}/{doc_id}.{ext}`. Record in nuova collection `chat_documents`. L'upload appende automaticamente un messaggio `[Allegato ricevuto: filename]` nella trascrizione `chat_conversations` e incrementa `documents_count` + `has_documents=true` sul lead. Nuovo endpoint admin `GET /api/admin/chat/documents/{session_id}` (lista) e `/download` (streaming). Frontend `ChatWidget.js`: aggiunto pulsante Paperclip nel composer che apre file picker, bubble ottimistico "Invio documento…" sostituito dopo upload con badge file (icona + nome), risposta automatica del proprietario "Grazie, ho ricevuto il documento 👍". Validation lato client coerente col backend (type + size). Verificato end-to-end: upload PDF → record in DB → messaggio in chat_conversations → lead aggiornato ✅.

- **2026-05-02** — Iteration 17: **Fix definitivo memoria chatbot**. La causa del "Ciao! Come va?" fuori contesto era: il blocco `KNOWN_INFO` dentro il system prompt cambiava ad ogni aggiornamento del lead → prompt_hash diverso → LlmChat ricostruito → storico interno perso → modello rispondeva come se fosse un primo messaggio. Fix in due punti: (a) Spostato `KNOWN_INFO` FUORI dal system prompt (insieme a `TODAY` e `AVAILABILITY_RESULT`): ora viene ricostruito fresh ogni turno e prependato al singolo `UserMessage` come `[KNOWN_INFO — do NOT ask these again]`. Il system prompt resta stabile. (b) **Replay storico dal DB**: quando la LlmChat è effettivamente ricostruita (cache miss o restart backend), il backend recupera gli ultimi 20 messaggi dalla collection `chat_conversations` e li prependa al primo user message come `[PREVIOUS CONVERSATION — for your memory only]`. In questo modo anche un riavvio non azzera la coerenza della chat. (c) Aggiunto anche un timestamp `[TODAY: YYYY-MM-DD]` automatico così l'AI sa sempre la data corrente per interpretare "il mese prossimo" / "a giugno" ecc. Verificato replicando la conversazione utente problematica: turno `"4"` → AI risponde "Perfetto, siete in 4" + preventivo corretto (8 notti 20-28 giugno, sconto settimanale applicato) ✅; turno successivo "hai dimenticato?" → AI si scusa in modo **concreto** citando esattamente le date sbagliate ✅; turno "procediamo" → conferma senza marcia-indietro lo stesso preventivo ✅.

- **2026-05-02** — Iteration 16: **Editor regole admin per l'AI + fix memoria**. (a) Nuova collection `ai_settings` (singleton `{id: 'global'}`) con 2 endpoint admin: `GET /api/admin/ai-settings` e `PUT /api/admin/ai-settings` per leggere/salvare il blocco `custom_rules`. Default pre-popolato con le 5 regole richieste dall'utente (controllo DB prima di dire libero/prenotato, niente domande superflue, "Verifico 5 secondi", no marcia-indietro dopo "procediamo", scuse concrete se sbaglia). Regole iniettate nel system prompt come sezione "## REGOLE PERSONALIZZATE DAL PROPRIETARIO — prevalgono sulle precedenti". Si applicano entro 30 secondi perché la cache LlmChat ha TTL basato su prompt_hash. (b) **Nuova tab admin "Assistente AI"** con textarea grande (18 righe, font mono), bottoni Salva / Ripristina default, counter caratteri, note spiegative su come scriverle bene. Accessibile da sidebar con icona Settings. (c) **Fix memoria chatbot critico**: `AVAILABILITY_RESULT` non è più iniettato nel system prompt (dove cambiava ad ogni messaggio → rigenerava LlmChat → perdeva lo storico), ma viene prependato al singolo `UserMessage` del turno. Il system prompt resta ora stabile per l'intera conversazione → `LlmChat` cached → memoria preservata. Verificato con test 3-turni: T2 ricorda che T1 era il primo messaggio, T3 ricapitola coerentemente il prezzo dato al T2.

- **2026-05-02** — Iteration 15: **Nuova persona del chatbot "proprietario su WhatsApp"**. Prompt di sistema riscritto seguendo specifica utente: (a) persona = proprietario di casa vacanza, non più "virtual concierge"; (b) tono informale, WhatsApp-like, max 3 frasi per messaggio, una domanda alla volta; (c) espressioni naturali suggerite ("Certo 😊", "Guarda,", "Allora…"); (d) divieto esplicito di elenchi puntati, grassetto, frasi da brochure e linguaggio formale; (e) raccolta dati NON più progressiva/a form: si lascia parlare l'ospite 2-3 turni poi si chiede UNA cosa alla volta in modo naturale ("Se vuoi ti controllo la disponibilità, come ti chiami?"); (f) descrizioni casa concrete (posizione, comodità) invece di atmosfera generica; (g) 2 esempi few-shot (GIUSTA vs SBAGLIATA) iniettati nelle rules per rinforzare la brevità. Meccanismi tecnici invariati: LEAD tag, SHOW_IMAGES tag, AVAILABILITY_RESULT server-verified, KNOWN_INFO dedup, cache LlmChat per prompt_hash. Verificato con conversazione di 4 turni: risposte 1-3 frasi, emoji leggere, no multi-topic dump.

- **2026-05-02** — Iteration 14: **Foto inline nella chat**. Quando l'ospite chiede "altre foto", "mi fai vedere la cucina", "show me pictures", l'AI emette un tag `<SHOW_IMAGES>current</SHOW_IMAGES>` (o `<SHOW_IMAGES>slug:N</SHOW_IMAGES>` per una casa specifica e count). Il backend parsa il tag, carica fino a 10 immagini dalla collection `properties`, e le restituisce come array `images[]` nel payload `ChatResponse`. Tag strippato dal reply, AI non inventa URL. Frontend `ChatWidget.js`: il bubble dell'assistente ora rende una griglia 2-col di thumbnails 96px con lazy-load, cliccabili per aprire la foto full-size in nuovo tab. Persistenza in localStorage: le foto restano visibili anche dopo refresh della pagina. Test end-to-end verificato: "Posso vedere altre foto?" → 3 immagini Unsplash di Villa Smeraldo mostrate inline. Schema `ChatResponseImage = {url, alt, property_slug}`.

- **2026-05-02** — Iteration 13: **L'AI concierge ora legge il calendario per davvero**. Fix richiesto dall'utente "l'assistente virtuale della chat non riesce ad accedere al calendario". (a) Nuovo helper `_format_blocked_dates(property_id)` che costruisce un blocco `BOOKING_CALENDAR` con tutti i range bloccati (bookings + ical_events) dei prossimi 180 giorni, ordinati per data, e lo inietta nel system prompt. (b) Flow rule step 3 riscritta: "you DO have access to the booking calendar … compare guest dates to the BOOKING_CALENDAR block". (c) **Controllo deterministico overlap lato Python** (non si fida dell'LLM per questo): `_extract_date_range(message, lang)` usa `dateparser` 1.4.0 + 3 pattern regex (IT+EN) per estrarre check-in/out dal messaggio utente; `_check_availability(...)` fa la query Mongo con il classico overlap `check_in < new.out AND check_out > new.in`; il risultato viene iniettato come blocco `AVAILABILITY_RESULT — SERVER-VERIFIED, TRUST THIS OVER THE CALENDAR BLOCK`. L'LLM deve solo raccontarlo al guest, non ragionare sulle date. (d) Test end-to-end con 24 prenotazioni reali bed-booking: 6–10 maggio → "già prenotate" ✅, 20–23 maggio → "sono libere ✓" + preventivo automatico €1.500 ✅, 18–24 (sconfina) → "già prenotate" con proposta 20–24 alternativa ✅, messaggio senza date → risposta normale senza falsi positivi ✅.

- **2026-05-02** — Iteration 12: **Code review fixes** (focused). (a) `ChatWidget.js`: 7 catch silenziosi (`catch (_) {}`) sostituiti con `console.warn` loggato e contesto, così eventuali problemi di localStorage/sessionStorage sono visibili in console dev. (b) `ChatWidget.js` e `AdminDashboard.js`: chiavi instabili `key={idx}` nei `.map()` dei messaggi → ora `key={m.ts+'-'+m.role}` (stable) con fallback su index solo per messaggi senza timestamp. (c) `tests/backend_test.py`: `random.randint(90, 365)` → `secrets.randbelow(276)+90` per conformità alle best-practice di generazione casuale. I check `is not None` sono stati lasciati invariati perché sono il pattern Python corretto per confronto con None (il review aveva falsato quel point). (d) Verificato `yarn lint` pulito + `ruff` pulito + smoke-test chat `/api/chat/message` 200.

- **2026-05-02** — Iteration 11: **CRM lead completo**. (a) **Dedup richieste AI**: ad ogni turno il backend carica il lead esistente dalla `chat_leads` e lo inietta nel prompt come blocco `KNOWN_INFO` + aggiunge la regola "DO NOT ask again" per i campi già presenti. L'AI ora saluta il guest per nome e salta le domande già fatte. Verificato: Turn 1 raccoglie "Lucia Bianchi", Turn 2 l'AI la chiama "Lucia" e non richiede il nome. (b) **PATCH endpoint esteso**: `/api/admin/chat/leads/{session_id}` accetta ora tutti i campi (guest_name, phone, email, origin_city, reason, dates, guests_count, status, note) via `exclude_unset=True` — permette correzioni manuali dall'admin. (c) **Admin CRM UI migliorata**: barra ricerca full-text (nome/email/telefono/città/motivo/note), filtri stato + casa, contatore filtrati/totali. (d) **Edit modale** con form completo (nome, telefono, email, città, motivo, date, ospiti, stato, note) → chiama il PATCH esteso. (e) **Export Excel (.xlsx)** nativo via SheetJS (`xlsx` 0.18.5) accanto al CSV esistente, con intestazioni italiane leggibili e larghezze colonne preset; esporta sempre il subset FILTRATO. (f) Nessuna modifica al consenso privacy (scelta utente: raccolta senza richiesta esplicita).

- **2026-05-02** — Iteration 10: **Bug fix produzione + iCal availability**. (a) **Auto-seed on startup**: `startup_app()` ora popola automaticamente il DB con le 5 case demo + account admin SOLO se la collection `properties` è vuota. Risolve il problema "proprietà sparite dopo il deployment" (il DB di produzione era separato dal preview e non aveva mai ricevuto il seed). Idempotente: se ci sono già proprietà personalizzate il seed è skippato. (b) **Bug critico iCal calendar**: l'endpoint `GET /api/properties/{id}/availability` cercava solo per UUID ma il frontend passa lo **slug** → restituiva 404 e il calendario non mostrava mai le prenotazioni sincronizzate. Fix: risolto via `$or: [{id}, {slug}]` e usato `real_id` in tutte le query sottostanti (bookings, ical_events, ical_syncs). Verificato end-to-end con feed bed-booking.com reale: 24 prenotazioni importate, 21 date bloccate correttamente visualizzate in rosso chiaro sul calendario maggio 2026.

- **2026-04-30** — Iteration 1 (handoff): MVP completo VacayStay, Stripe, WhatsApp, Admin base, seed 5 properties.
- **2026-04-30** — Iteration 2: Rebrand → TerracitoAppartments. iCal scheduler bidirezionale (APScheduler + export pubblico). Upload documenti d'identità ospiti via Emergent Object Storage. 23/23 backend tests green.
- **2026-04-30** — Iteration 3: Light Mediterranean theme (Cormorant Garamond, terracotta su cream). Admin Property CRUD completo con upload immagini. 36/36 backend tests green.
- **2026-04-30** — Iteration 4: Riposizionamento copy. Rimossi tutti i riferimenti a "lusso/luxury/esclusivo/elegante". Hero, feature card, footer, descrizioni seed e meta SEO ora puntano su "case ben curate, sempre pulite, accoglienti, comfort autentico". Aggiornate 5 proprietà già seedate nel DB.
- **2026-04-30** — Iteration 5: Code-review fixes. Backend: bare except → tipizzato; credenziali test via env; Response spostate dentro try (no più pseudo-warning unbound vars); rimosse var inutilizzate. Frontend: useCallback/useMemo per stabilizzare reference (AuthContext value, fetchProperty, calculatePrice, fetchData admin, fetchDocs, pollPaymentStatus, calendarDisabledRules); useEffect deps complete; key React stabili al posto degli index (gallery thumbs, review stars, season rows con _key uuid); empty catch sostituiti con log+toast quando rilevanti. Bug TDZ scoperto e fixato (PropertyDetailPage useMemo order). 36/36 backend test green; UI verificata.
- **2026-05-02** — Iteration 6: Galleria contenuta e nitida (max-w-7xl + aspect 16:9, helper heroImageUrl per Unsplash/Pexels HD). Logout + "Torna al sito" in admin sidebar. "Registrati" rimosso da navbar mobile + rotta `/register` disabilitata. Filtro città dropdown dinamico in PropertiesPage. Sezione Posizione con mappa OpenStreetMap su PropertyDetailPage + link "Apri in Google Maps". Upload multiplo (max 5 foto) in PropertyFormDialog con upload parallelo. Pulsante "Trova coordinate dall'indirizzo" nel form admin (Nominatim, no API key). Badge sconti soggiorno medio-lungo (weekly/monthly_discount) accanto al prezzo notte sulla pagina dettaglio. Riposizionamento copy: rimossi residui "Collezione esclusiva", "Le Nostre Proprietà".
- **2026-05-02** — Iteration 7: **AI Chat Concierge** integrata. Backend: nuovi endpoint `/api/chat/message`, `/api/admin/chat/conversations`, `/api/admin/chat/conversations/{id}` con Claude Haiku 4.5 via emergentintegrations. Modello WelcomeManual (check-in/out, WiFi, parcheggio, regole, trasporti, emergenze, tips, FAQ libere) aggiunto a Property. Sensitive info (WiFi password + indirizzo esatto) rivelate solo con booking_code valido. Conversazioni persistite in MongoDB collection `chat_conversations`. Frontend: nuovo `ChatWidget.js` flottante (bottom-20 right-5 per non collidere col badge Made-with-Emergent), full-screen su mobile, quick prompts (Check-in / Wi-Fi / Indirizzo / Parcheggio), typing indicator, link `tel:` quando AI rimanda all'host. Auto-detect property dal pathname `/property/:slug`. localStorage persistence di session_id, history e booking_code. Tab "Chat AI" in admin con lista sessioni + viewer transcript. Errore budget LLM gestito con messaggio chiaro all'admin.
- **2026-05-02** — Iteration 9: **Chat property-scoped, mobile fix, sales flow + lead capture**. (a) `ChatWidget` ora si nasconde fuori da `/property/:slug` (short-circuit via `isOnPropertyPage`). (b) **Mobile fix**: panel usa `100dvh` anziché `inset-0`/`100vh`, `env(safe-area-inset-bottom)` per iPhone home indicator, `overflow:hidden` su body mentre la chat è aperta per evitare scroll del background quando si apre la tastiera. (c) **Sales flow strutturato** nel system prompt: saluto caldo → chiesta date → conferma disponibilità (senza promettere) → preventivo dettagliato (base×notti + pulizie + cauzione con formula) → 2-3 punti di forza dalla casa → chiusura progressiva raccogliendo nome+cognome, poi telefono, poi email, poi motivo, poi città di provenienza in step naturali. (d) **Pricing esteso** nel data block: cleaning_fee, security_deposit, extra_guest_fee, weekend_price, weekly/monthly_discount. (e) **Lead capture** via sentinel tag `<LEAD>{...}</LEAD>` appeso a ogni reply dall'LLM; il backend estrae il JSON, upserta nella collection `chat_leads` (campi: guest_name, phone, email, reason, dates, guests_count, origin_city, property_id/slug/title, status, language, created_at, last_update, note) e strippa il tag prima di inviare al guest. (f) Nuovi endpoint admin: `GET/PATCH/DELETE /api/admin/chat/leads[/<session_id>]` — update status (new/contacted/converted/lost) e note. (g) Backup JSON ora include `chat_leads`. (h) **Nuova tab admin "Lead Chat"** in `AdminDashboard`: tabella con nome, telefono (tel: link), email (mailto:), città, motivo, date, casa, stato editabile inline, data ultimo aggiornamento, azioni "vedi conversazione" e "elimina", + bottone **"Esporta CSV"** con BOM UTF-8 per Excel/Numbers. Verificato: conversazione di 2 turni crea un record lead completo con guest_name=Mario Rossi, phone=+393331234567, origin_city=Milano, reason=family vacation, dates=10-15 luglio 2026, guests_count=4.

## Pending / Future
- **P0 deferred (code review)** Migrazione token JWT da `localStorage` a **httpOnly cookies** (AuthContext + backend session + CSRF + CORS `credentials:'include'`). Scelta: rimandata a iterazione dedicata via `integration_playbook_expert_v2`, cambio architetturale non adatto come patch di review.
- **P1 deferred (code review)** Split dei componenti grossi: `AdminDashboard.js` (~1.4k righe, 291 complexity) → `BookingsTab`, `PropertiesTab`, `LeadsTab`, `ChatTab`, `SyncTab`, `ContactsTab`, `DashboardTab`. `PropertyFormDialog.js` → `BasicInfoForm`, `TranslationsForm`, `LocationForm`, `PricingForm`, `AmenitiesForm`, `PhotosForm`, `SeasonsForm`, `ExtrasForm`, `WelcomeManualForm`. `ChatWidget.js` → hook `useChatSession`, `useChatHistory` + sotto-componenti `ChatLauncher`, `ChatPanel`, `ChatMessages`, `ChatComposer`, `BookingCodeField`. `PropertyDetailPage.js` → `Gallery`, `AvailabilityCalendar`, `BookingSidebar`, `LocationMap`. Rischio regressioni alto → serve iterazione con testing agent full.
- **P1 deferred (code review)** Refactor complessità backend: `calculate_price()` (C19) → `apply_seasonal_pricing`, `apply_length_discounts`, `calculate_fees`. `sync_single_feed()` (C19) → `parse_feed`, `validate_events`, `upsert_events`. `chat_message()` (C20) → `prepare_prompt`, `call_llm`, `persist_turn`. `_format_welcome_manual()` (C41) e `_build_system_prompt()` (121 lines) → template builders per sezione. `_extract_and_store_lead()` (C17) → extract/validate/store split.
- **P1** Multi-lingua IT/EN dinamica (il toggle esiste ma alcune stringhe sono ancora hardcoded; rivedere AdminDashboard, BookingPage, LegalPages)
- **P2** Email automatiche pre/post-soggiorno + invio contratto (Resend o SendGrid)
- **P2** Sistema upselling in fase di prenotazione (selezione extras prima del pagamento)
- **P3** DocumentUpload visibile anche prima del completamento Stripe (gating su payment status fa sì che non si veda in dev)
- **P3** Cleanup job per `property_images` orfane.

## Test Credentials
Vedi `/app/memory/test_credentials.md`
